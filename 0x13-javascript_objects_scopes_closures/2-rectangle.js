#!/usr/bin/node
/*
 * Rectangle that defines rectangle width and height
 */
class Rectangle {
	constructor(w, h) {
		this.width = w;
		this.height = h;
	}
	if (typeof w === 'number' && w > 0 && typeof h === 'number' && h > 0) {
	}
}
module.exports = Rectangle;
